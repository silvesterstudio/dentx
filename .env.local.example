# ── Copy this file to ".env.local" and fill in the real values. ──
# .env.local is git-ignored and stays on the server — these keys never reach the browser.

# Telegram notifications for the review page's PRIVATE (low-rating) feedback.
# When an unhappy customer submits the private form at /feedback, the clinic is
# pinged instantly. (Same bot you used before — the values likely already exist
# in your Vercel project env.)
#   TELEGRAM_BOT_TOKEN = token from @BotFather after /newbot
#   TELEGRAM_CHAT_ID   = your chat id (message the bot, then open
#                        https://api.telegram.org/bot<token>/getUpdates and read result[].message.chat.id;
#                        for a group, add the bot to it and use the negative group id)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
